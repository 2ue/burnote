# Burnote Frontend Configuration

# Frontend Server Port (dev only)
VITE_PORT=3500

# Backend API URL
# For development: http://localhost:3501 (direct connection)
# For Docker: /api (relative path, nginx proxies to backend)
VITE_API_URL=http://localhost:3501
