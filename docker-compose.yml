version: '3.8'

services:
  burnote:
    build: .
    ports:
      - "3500:3500"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:admin123}
    volumes:
      - burnote-data:/app/data
    restart: unless-stopped

volumes:
  burnote-data:
