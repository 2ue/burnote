# Burnote Backend Configuration

# Database
# For development: file:./dev.db
# For production (Docker): file:/app/data/burnote.db
DATABASE_URL="file:./dev.db"

# Server
# For development: 3501
# For production (Docker): 3000
PORT=3501
NODE_ENV=development

# Admin Authentication
# IMPORTANT: Set a strong password in production
# If not set, admin features will be completely disabled
# Supports both plain text (dev only) and bcrypt hashed passwords
ADMIN_PASSWORD=change-me-to-secure-password

# CORS Configuration
# Set to your frontend URL
# For development: http://localhost:3500
# For production: https://yourdomain.com or http://localhost
CORS_ORIGIN=http://localhost:3500
