# ============================================
# Burnote Docker Runtime Configuration
# ============================================
# This file contains ALL environment variables that can be
# configured when running the Docker container.
#
# Usage:
#   docker run --env-file .env.docker -p 80:80 burnote:latest
#
# Or with docker-compose:
#   Put these variables in your docker-compose.yml
# ============================================

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL (as seen from browser)
# - For same-domain deployment: /api (default, uses nginx proxy)
# - For separate domains: https://api.yourdomain.com
# - For local testing: http://localhost:3000
VITE_API_URL=/api

# ============================================
# Backend Configuration
# ============================================

# Database Configuration
# - SQLite file path (recommended to use volume mount)
# - Default: file:/app/data/burnote.db
DATABASE_URL=file:/app/data/burnote.db

# Server Port
# - Internal backend port (nginx proxies to this)
# - Default: 3000 (don't change unless you know what you're doing)
PORT=3000

# Node Environment
# - production (default for Docker)
# - development (enable debug logging)
NODE_ENV=production

# CORS Configuration
# - Allowed origins for CORS
# - Default: * (allow all origins)
# - For production: https://yourdomain.com
# - Multiple origins: https://domain1.com,https://domain2.com
CORS_ORIGIN=*

# ============================================
# Admin Authentication (Optional)
# ============================================

# Admin Password
# - If not set, admin features are COMPLETELY DISABLED
# - Supports both plain text (dev only) and bcrypt hashes
# - To generate bcrypt hash:
#   node -e "console.log(require('bcrypt').hashSync('your-password', 10))"
#
# IMPORTANT: Set a strong password in production!
ADMIN_PASSWORD=

# Example with bcrypt hash:
# ADMIN_PASSWORD=$2b$10$YourBcryptHashHere

# ============================================
# Example Production Configuration
# ============================================
# Uncomment and modify for production use:
#
# VITE_API_URL=/api
# DATABASE_URL=file:/app/data/burnote.db
# PORT=3000
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com
# ADMIN_PASSWORD=$2b$10$...your-bcrypt-hash...
